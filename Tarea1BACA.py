def compareStrings(str a, str b):
    try:
        largoa = str(len(a))
        largob = str(len(b))
        frase1 = "El String1 tiene " + largoa + "caracteres."
        frase2 = "El String2 tiene " + largob + "caracteres."
        print(frase1)
        print(frase2)
        if (largoa > largo b):
            print("El String1 es más largo.")
        else if (largob > largoa):
            print("El String2 es más largo.")
        else:
            print("Los dos strings poseen el mismo largo")
    except:
        print("Hubo un error, inténtelo otra vez")

while(true)
    string1 = input("String1: ")
    string2 = input("String2: ")
    compareStrings(string1, string2)
    answer = input ("¿Desea hacer otra comparación? (Y/N): ")
    
            
        
